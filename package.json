{
  "name": "gravryn-website",
  "version": "1.0.0",
  "scripts": {
    "build": "curl -fsSL https://deno.land/install.sh | sh && export PATH=\"$HOME/.deno/bin:$PATH\" && deno task build",
    "install": "curl -fsSL https://deno.land/install.sh | sh"
  }
}