{
  "name": "grav-website",
  "version": "1.0.0",
  "scripts": {
    "build": "bash -c 'curl -fsSL https://deno.land/install.sh | sh && export PATH=\"$HOME/.deno/bin:$PATH\" && deno install && deno task lume'",
    "install": "curl -fsSL https://deno.land/install.sh | sh"
  }
}